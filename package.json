{
  "name": "bms-monitor",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm i && npm i --prefix frontend && npm i --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:electron": "npm run electron --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "build:frontend": "npm run build --prefix frontend",
    "build:electron": "npm run build --prefix frontend && npm run dist --prefix frontend",
    "cap:build": "npm run cap:build --prefix frontend",
    "cap:sync": "npm run cap:sync --prefix frontend",
    "cap:android": "npm run cap:build --prefix frontend && npm run cap:android --prefix frontend",
    "cap:ios": "npm run cap:build --prefix frontend && npm run cap:ios --prefix frontend",
    "cap:run:android": "npm run cap:build --prefix frontend && npm run cap:run:android --prefix frontend",
    "cap:run:ios": "npm run cap:build --prefix frontend && npm run cap:run:ios --prefix frontend"
  },
  "devDependencies": {
    "electron": "^42.2.0",
    "electron-builder": "^26.8.1",
    "vite-plugin-pwa": "^1.3.0"
  }
}
